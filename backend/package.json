{
  "name": "backend",
  "version": "0.2.0",
  "description": "Wasm-Sentry backend",
  "main": "index.js",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json && node -e \"require('fs').copyFileSync('src/db/schema.sql','dist/db/schema.sql')\"",
    "start": "node dist/index.js"
  },
  "author": "Amogh",
  "license": "ISC",
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "@wasm-sentry/core": "*",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0"
  },
  "type": "module"
}

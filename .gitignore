# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo
# tsc emitting next to the sources it compiled, which it should never do
backend/**/*.js
!backend/**/*.config.js

# env / local
.env
.env.*
*.local
data/
*.sqlite
*.sqlite-journal

# os / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# logs
logs/
*.log
npm-debug.log*

# --- private working material (never pushed) ---
docs/private/
*.pdf

# a trained model is dropped in by whoever trains one; never committed, because
# a model is only as honest as the corpus behind it and that corpus is not here
extension/public/model.json
corpus/

# generated test fixtures
testbed/*.wasm

# copied in by scripts/prepare-testbed.mjs so standalone.html can load it
testbed/injector.js
